import {SafeAreaView, Text} from 'react-native';
import styled from 'styled-components/native';


const App = () => {
  return (
    <SafeAreaView>
      <Text>Testando</Text>
    </SafeAreaView>
  );
};

export default App;
